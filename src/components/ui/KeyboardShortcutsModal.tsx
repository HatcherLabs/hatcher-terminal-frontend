"use client";

/**
 * @deprecated This component is kept for backward compatibility.
 * Keyboard shortcuts are now fully handled by `useKeyboardShortcuts` hook
 * and displayed via `ShortcutsModal` (rendered in TerminalLayout).
 * This component renders nothing — remove the import when convenient.
 */
export function KeyboardShortcutsModal() {
  return null;
}
